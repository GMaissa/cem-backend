Hi all !

The project development platform "{{ instance.name() }}" has been stopped{% if user %} by {{ user.fullname }}{% endif %}.

In case you need to start it, you can do so from http://{{ host_dashboard }}.

Regards,
