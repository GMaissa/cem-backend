Hi all !

The project development platform "{{ instance.name }}" has been started{% if user %} by {{ user.fullname }}{% endif %}.

Its public IP (used for ssh connection) is now : {{ instance.publicip }}

Full instance information are available from http://{{ host_dashboard }}.

Regards,
